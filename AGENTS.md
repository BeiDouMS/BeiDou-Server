# AGENTS.md

本文件是仓库级 Agent 指令入口，适用于整个仓库。具体规则不在此重复维护，请按以下顺序读取并遵守：

1. 必须读取 [`CLAUDE.md`](CLAUDE.md)，将其中的项目架构、开发命令、编码规范和注意事项视为所有编码 Agent 的共享指令；其中针对 “Claude” 的表述同样适用于当前 Agent。
2. 如果 [`CLAUDE.local.md`](CLAUDE.local.md) 存在，必须继续读取。它包含本机环境和个人工作流约定；与 `CLAUDE.md` 冲突时，以 `CLAUDE.local.md` 为准。
3. 如果子目录中存在更具体的 `AGENTS.md`，进入该目录工作时还须遵守子目录指令；冲突时以作用域更具体的指令为准。

`CLAUDE.local.md` 是可选且不提交的本地文件。文件不存在时，直接以 `CLAUDE.md` 为准，不应因此阻塞任务。

## 装备词条逻辑接入流程

新增或调整装备词条时，必须同时检查配置、实例持久化、运行时效果和玩家展示四个层面，不能只修改随机生成器：

1. **数据库配置**：新增结构或初始数据必须创建新的 Flyway 迁移版本，不修改已经发布的迁移。词条类型、装备品质、词条 T 等阶和词条命名是独立维度；命名键必须按 `affix_code + affix_tier` 配置，不能只按 T 等阶共用一个名称。
2. **实例链路**：确认 `Equip`、`ItemFactory`、`inventoryequipment` 和 `inventory_equipment_affix` 的加载、保存、复制、交易、商店和删除流程都保留装备品质、词条类型、T 等阶和值。固定属性词条必须通过独立的词条贡献参与最终属性计算；混沌卷轴、升级卷轴等装备改造只能修改装备本体值，不能把词条贡献再次写入本体。
3. **运行时效果**：固定属性词条合并到装备属性；倍率或条件效果接入统一的服务端计算入口，避免在技能、掉落等多个入口重复计算。新增效果必须明确是否已实际生效，不能仅生成并持久化。
4. **玩家展示**：原版 v83 客户端不会根据装备实例显示动态名称，不能只改 WZ/XML。兼容方案使用拾取提示和 `@inspect`/`@affix` 命令；客户端 Tooltip 需要额外修改客户端代码和封包。
5. **展示格式**：拾取提示与查看命令必须共用格式化逻辑，并显示装备品质、词条类型、实际值和 T 等阶。人类可见文本走 i18n；缺少特定命名翻译时只能使用明确的降级文本，不能显示数据库键。
6. **验证**：至少使用 JDK 21 执行 `mvn -pl gms-server -DskipTests compile`，并执行 `git diff --check`。涉及迁移时还要检查新数据库和已执行旧迁移数据库的升级顺序。

推荐的玩家交互流程：

```text
拾取装备 -> 聊天框显示词条摘要
@inspect  -> 列出装备栏位和词条数量
@inspect 1 或 @affix 1 -> 显示指定装备的完整词条
```
